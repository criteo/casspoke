if (!file('build.gradle').text.contains('com.criteo.moab-module')) {
    // This build.gradle file was not translated to JMOAB2. We don't include it
    project(':probes:casspoke').buildFileName = 'untranslated.gradle'
    moabMavenWalker.includeAll(this)
}

